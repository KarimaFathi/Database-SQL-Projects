SELECT distinct Makes.MakeName
FROM   Makes INNER JOIN
             VehiculeDetails ON Makes.MakeID = VehiculeDetails.makeID where Engine_CC in (select distinct top 3 Engine_CC from VehiculeDetails order by Engine_CC desc) order by MakeName;
