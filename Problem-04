SELECT Makes.MakeName, count(*) as VehicleNumbers
FROM   Makes INNER JOIN
             VehiculeDetails ON Makes.MakeID = VehiculeDetails.makeID
WHERE year between 2000 and 2020 group by MakeName order by VehicleNumbers desc;
