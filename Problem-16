SELECT distinct Makes.MakeName,DriveTypes.DriveTypeName
FROM   Makes INNER JOIN
             VehiculeDetails ON Makes.MakeID = VehiculeDetails.makeID INNER JOIN
             DriveTypes ON VehiculeDetails.DriveTypeID = DriveTypes.DriveTypeID where DriveTypeName = 'FWD';
